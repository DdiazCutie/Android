--BASE DE DATOS
CREATE DATABASE ImpaktoIsil
ON
(NAME = ImpaktoIsil_dat,
FILENAME = 'E:\PROYECTOd_APP\ImpaktoIsil_dat.mdf',
SIZE = 100,
MAXSIZE = UNLIMITED,
FILEGROWTH = 64)
LOG ON
(NAME = ExP_LOG,
FILENAME = 'E:\PROYECTOd_APP\ImpaktoIsil_log.ldf',
SIZE = 50,
MAXSIZE = UNLIMITED,
FILEGROWTH = 64)
GO

--TABLAS 
--EMPLEADO
CREATE TABLE Empleado(
IdEmpleado SMALLINT NOT NULL,
NombreEmpleado VARCHAR(100) NOT NULL,
ApellidoEmpleado VARCHAR(100) NOT NULL ,
TipoDocumento VARCHAR(20) NULL,
NroDocumentoEmpleado VARCHAR(15) NULL,
IdArea TINYINT NOT NULL,
EmpleadoActivo BIT NOT NULL,
CONSTRAINT PK_IdEmpleado PRIMARY KEY (IdEmpleado))

ALTER TABLE Empleado
ADD CONSTRAINT FK_IdArea
FOREIGN KEY (IdArea) REFERENCES Area(IdArea);
